# Personal health data — never commit. Covers the account export (raw/),
# the API pull (raw/api/) and the probe's raw samples (raw/api_samples/).
raw/
!raw/.gitkeep
*.duckdb
*.duckdb.wal

.env
# Garmin OAuth tokens, if anyone points $GARMIN_TOKENS inside the repo.
.garminconnect/
garmin_tokens.json

.venv/
__pycache__/
*.pyc
.pytest_cache/
.DS_Store

# Build artefacts from `pip install -e .`
*.egg-info/

# Generated dashboard page (rebuild with: python notebooks/eval_dashboard.py)
notebooks/eval_dashboard.html
